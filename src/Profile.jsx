import React from "react";
import axios from "./api/axios";
const UserProfileURL = "/api/accounts/user/:id";

export default function Profile({}) {
    return (
        <>
            <h1>My Profile</h1>
        </>
    );
}
